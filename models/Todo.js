export default class Todo {
  constructor(title, content, status, date, userid) {
    this.title = title;
    this.content = content;
    this.status = status;
    this.userid = userid;
    this.date = date;
  }
}
