export default class User {
  constructor(id, username, surname, email, photoUrl) {
    this.id = id;
    this.username = username;
    this.surname = surname;
    this.email = email;
    this.photoUrl = photoUrl;
  }
}
